# 現行計画サマリ

更新日: 2026-05-18 JST

## 現在地

- 現在版は `Ver02.5-v7`。
- 現在フェーズは `Phase 0` 本番観測 + `Phase 1A` 観測紙トレード + `Phase 1B-lite` 限定検証。
- 正式 `Phase 1B` と自動取引は未達。`trade_execution_gate=pass=0件`、`paper_orders planned=0件`。
- 最新基準は `feedback_daily_sync_20260518.md`: 完了 47 件、近似PF 0.73、勝率 46.8%。

## 実装済みとして archive へ移した計画

- `Phase 1B-lite`: 専用CSVで観測する限定検証レーン。正式 Phase 1B ではない。
- 通知ランク再設計: `執行候補` と `監視・実行不可` を表示上で分離済み。
- ロング誤判定と下落取り逃し対策: `market_map`、失敗ブレイク型、文言誤読対策を導入済み。
- `Ver02.5-v7` 先行実装パック: 15分足執行チェック、`trend_flip_confirmed_up` 弱評価化、詳細HTML/CSV保存を導入済み。
- AI事後評価運用: 日次 `daily-sync` と AI post review の運用として実装済み。

## まだ進行中の判断

- `Phase 1B-lite` は 5 件で、10〜15 件の観測条件に未達。
- `trend_flip_confirmed_up` は 16 件でも弱く、強評価や gate 緩和には使わない。
- `support_to_resistance_flip` は相対的に有効で、下方向材料として継続観測する。
- `Ver02.5-v7` の `execution_precision_*` が live の result / CSV / 詳細HTMLに入るか、次回サイクルで確認する。

## 次に作るべきもの

1. `Phase 1B-lite` のサンプルを 10〜15 件まで増やし、TP1先行率、近似PF、平均MFE/MAEを見る。
2. `15分足 執行チェック` による `ready -> watch` 降格が過剰でないか確認する。
3. AI backlog 75 件を見ながら、`AI_POST_REVIEW_DAILY_MAX` を 4 のまま維持するか、6/8 へ増やすか判断する。
4. 正式 `Phase 1B` は `trade_execution_gate=pass` と `paper_orders planned` が出るまで上げない。

## 現行で見る正本

- `../NEXT_TASK.md`: 次にやること。
- `../開発ロードマップ.md`: フェーズと大型節目。
- `Phase1条件の見方.md`: Phase 1A / 1B / lite の意味。
- `マイルストーン定義.md`: Ver03 以降の昇格条件。
- `計画全体タイムライン.html`: 人向けの全体位置づけ。
