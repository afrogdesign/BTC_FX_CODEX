# NEXT TASK TRACKER

## 現在の状況
- 常駐実行は `launchd` で動作中。
- 定時実行は毎時 `:05` に変更済み。
- 通知条件は 2026-03-10 に「気づき重視」へ調整済み。
- AI判定は API 接続自体は成功確認済みで、現在はタイムアウト調整後の様子見段階。
- 要約生成は `AI_SUMMARY_TIMEOUT_SEC=10` に延長済み。
- 常駐プロセスは再起動済みで、`monitor.pid` と `launchctl` の PID 同期も修正済み。
- 次回以降は AI 失敗時の詳細が `logs/errors/*ai_*_error.log` に残るよう対応済み。
- メール本文は、数値の羅列より意味が伝わる日本語へ改善中。
- 打ち合わせノートは、`今の状況`・`現在の動向`・`今後のやるべきこと` の3ブロックで簡潔に追記する方針。
- 他案件向けには `Global_BOX/打ち合わせノートテンプレート.md` を共通ひな形として使える状態にした。
- 現在の通知しきい値:
  - `CONFIDENCE_LONG_MIN=60`
  - `CONFIDENCE_SHORT_MIN=65`
  - `CONFIDENCE_ALERT_CHANGE=7`
  - `ALERT_COOLDOWN_MINUTES=30`

## 次のタスク
- 1. 次回の毎時 `:05` 実行後に、`logs/heartbeat.txt` と `logs/last_result.json` が正常更新されるか確認する。
- 2. `logs/last_result.json` の `ai_advice` が復帰しているか確認する。
- 3. 失敗していた場合は `logs/errors/` を見て、タイムアウト再発か別要因かを切り分ける。
- 4. 次回メール本文が、日本語として自然で判断しやすいか確認する。
- 5. 数サイクル分の通知回数を見て、多すぎるならしきい値を再調整する。
- 6. 落ち着いたら、反発・反落の初動専用アラート案を整理する。

## 完了条件
- 設定修正後の常駐実行で AI判定が復帰するか、または失敗理由がログで明確になること。
