# NEXT TASK TRACKER

## 現在の状況
- MEXCベースの既存判定に、Binance公開APIを使う市場構造取得と位置フィルター層を追加済み。
- `prelabel`、`location_risk`、`risk_flags`、流動性・清算・OI/CVD・板の各項目を result payload に追加済み。
- 通知、要約、CSVログ、バックテストも新しい位置判定に追従済み。
- 清算は有料ヒートマップではなく、直近清算イベントの集計による近似実装。
- OpenAI API を使う手動 `run_cycle` は実施済み（`WAIT_FOR_SWEEP` 応答を確認）。

## 次のタスク
- 1. 定時実行サイクルでも `prelabel` と市場構造項目が安定して入るか、`logs/last_result.json` を複数回確認する。
- 2. 位置フィルターが強すぎるか弱すぎるか、`location_risk` と `risk_flags` を数サイクル分観察して閾値を調整する。
- 3. Binance WS 清算イベントが0件になる時間帯を減らすため、イベント蓄積（ローカル保存）を追加する。
- 4. 余裕が出たら、清算イベントのローカル蓄積からクラスター精度を上げる。
- 5. Ver01/Ver02 を分離して運用するため、デプロイ先・launchdラベル・ログ保存先の分割案を具体化する。

## 完了条件
- 市場構造データが本番サイクルで安定取得でき、`prelabel` と通知文が実運用で使える粒度になっていること。
