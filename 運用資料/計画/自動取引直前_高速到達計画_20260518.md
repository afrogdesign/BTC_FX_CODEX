# 自動取引直前 高速到達計画

更新日: 2026-05-26 JST

## 結論

自動取引の直前までは、正式 `Phase 1B` の発生を待たずに進める。
ただし、実弾発注はまだ禁止したままにする。

現時点では、`watch` / `Phase 1A` / `Phase 1B-lite` / `market_map` の有望候補を `opportunity_gate` で拾い、`paper_positions.csv` で紙ポジションとして自動記録・後追い評価する骨格はできている。

次の重点は、候補数を増やすことではなく、`sl_hit` に偏る弱い紙候補を分離し、勝てる型だけを残すことである。

## 現在の前提

- 現在の作業ブランチ: `ver02.6-v2`
- 運用本体の参照ブランチ: `ver02.5-v8`
- 現在版: `Ver02.5-v8` 稼働中 / `ver02.6-v2` で設計・仕様化中
- 現在フェーズ: `Phase C` 実装済み・観測中、`Phase D` 初期段階
- 既存実装: `position_sizing`、`exit_manager`、`trade_execution_gate`、`phase1_observation_gate`、`phase1b_lite_gate`、`market_map`、`opportunity_gate`、`paper_positions.csv`、AI事後評価、日次レポート。
- 直近課題: `trade_execution_gate=pass` と `paper_orders planned` が 0 件で、厳格 gate 待ちだけでは前進が遅い。
- 追加課題: `paper_positions.csv` では `sl_hit` 偏重が残り、特に `long`、高 `wait`、低 `execution`、`trend_flip_confirmed_up` が弱い。
- 新方針: 正式 gate は残しつつ、紙候補の品質ガードで負け型を分離し、Phase D の型別選別へ進める。

## 実装済みの骨格

1. `opportunity_gate`
   - `phase1_observation_gate=pass`、`phase1b_lite_gate=pass`、有効な `market_map` 型を紙候補として扱う導線。
   - hard no-trade、Funding禁止、ATR極端、data quality 不良は除外する。
   - `trade_execution_gate=pass` は正式候補として別格に扱うが、現在は 0 件。

2. `paper_positions.csv`
   - 保存先: `logs/csv/paper_positions.csv`
   - 役割: 自動取引直前の紙ポジション台帳。
   - `ready` は `opened`、それ以外の有望候補は `pending` として記録する。
   - `pending -> opened -> closed` の状態管理を持つ。
   - 正式 `paper_orders.csv` とは混ぜない。

3. 通知表示の分離
   - 正式 gate 通過のみ `執行候補`。
   - `opportunity_gate=pass` は `紙実行候補・実弾不可`。
   - ユーザーが実弾候補と誤読しない文言にする。

4. 日次レポート
   - `opportunity_gate=pass`
   - `paper_positions` 件数
   - `pending/opened/closed` 状態
   - `exit_status` 内訳
   - `opportunity_type` 内訳
   - closed 件数、勝率、平均R、簡易PF
   - missed_opportunity と古い pending
   - 未記録の欠落件数

## Phase B-C: 紙ポジション状態遷移

`paper_positions.csv` は候補台帳ではなく、実弾なしの紙ポジション管理として扱う。
既存列は維持し、`opened_at_jst`、`closed_at_jst`、`exit_status`、`exit_reason`、`close_price`、`tp1_hit_at_jst`、`tp2_hit_at_jst`、`sl_hit_at_jst`、`timeout_at_jst`、`mfe_atr`、`mae_atr`、`realized_r`、`missed_opportunity`、`missed_reason`、`last_evaluated_at_utc` を使う。

- `position_status` は `pending` / `opened` / `closed` の 3 値。
- `opportunity_gate=pass` を `signal_id` で upsert し、既存行の状態は破棄しない。
- `ready` は即 `opened`、それ以外は `pending`。
- `pending` は entry price に触れたら `opened`。
- entry に触れないまま TP 方向へ走ったら `closed / missed_opportunity`。
- `opened` は TP2 / SL / timeout で `closed`。
- 判定は 15分足の `high` / `low` を使う。long は entry/tp が `high >= price`、SL が `low <= stop`。short は entry/tp が `low <= price`、SL が `high >= stop`。
- 同一 candle で TP と SL が両方成立した場合は、保守的に SL 優先。
- TP1 到達後、`breakeven_after_tp1=true` なら実効SLを entry price へ引き上げる。
- timeout は `opened_at + timeout_hours`、未設定時は 12h。
- pending は signal から 24h 未到達なら `closed / entry_not_reached`。

## 現在の主要数値

- `trade_execution_gate=pass`: 0件
- `paper_orders planned`: 0件
- `phase1_active=true`: 2件
- `paper_positions.csv`: closed 19件 / `sl_hit=12件` / `missed_opportunity=5件` / `tp2_hit=1件` / `timeout=1件`
- `market_map_opportunity`: 9件 / 勝率 11.1% / 平均R 0.26 / 簡易PF 1.57
- 累計診断の `market_map_opportunity`: 97件 / 平均R 0.36 / 簡易PF 1.97
- `long`: 18件 / 平均R -0.51 / 簡易PF 0.29
- `wait>=60`: 39件 / 平均R -0.16 / 簡易PF 0.74
- `wait>=80`: 7件 / 平均R -0.84 / `sl_hit=6件`
- `execution<20`: 44件 / 平均R -0.02 / `sl_hit=29件`
- `trend_flip_confirmed_up`: 32件 / 勝率 41.2% / wrong_rate 28.1% / 平均MFE24h 2.50 / 平均MAE24h 10.85
- 紙ポジション診断の `trend_flip_confirmed_up`: 7件すべて `sl_hit`

## 現在の実装順

1. 品質ガード
   - active spec: `chatgpt/specs/active/20260526_entry_wait_trend_flip_quality_guard.md`
   - `src/trade/opportunity_gate.py` に paper quality blocker を追加する。
   - `wait>=80`、`execution<20`、`long + wait>=60 + execution<25`、`long + trend_flip_confirmed_up` を弱い紙候補として分離する。

2. レポート導線
   - quality blocker 別件数を追えるようにする。
   - `paper_opportunity_diagnostics` と daily-sync で、抑制後の `sl_hit` 比率と `missed_opportunity` の増減を見る。

3. Phase D 判定
   - `support_to_resistance_flip` など有効な market_map 型が維持されているか確認する。
   - `long` / `trend_flip_confirmed_up` がまだ弱いか確認する。
   - `trade_execution_gate=pass` と `paper_orders planned` が自然発生するか見る。

4. 次段階
   - 品質ガード後の型別PFと取り逃し率を見て、score / gate / entry / SLTP のどこを触るか再設計する。
   - SL/TP 倍率や RR 計算は、品質ガード後の `sl_eval=too_tight` 群だけを見て判断する。

## 完了済み条件

- `opportunity_gate` が CSV / JSON / shadow に保存される。
- `paper_positions.csv` が自動生成される。
- 通知上、正式 `執行候補` と紙候補が混ざらない。
- 日次レポートで紙候補の件数と状態を確認できる。
- 紙ポジションの `pending -> opened -> closed` と TP2 / SL / timeout / missed / entry_not_reached の後追い評価が動いている。

## まだ未完了の条件

- `trade_execution_gate=pass` が自然発生していない。
- `paper_orders planned` が自然発生していない。
- `Phase 1B-lite` は正式 `Phase 1B` へ昇格していない。
- `sl_hit` 偏重が残っている。
- quality blocker 後の成績がまだ未確認。
- kill switch / blackout / 実弾直前の安全装置はまだ未整備。

## 禁止事項

- 実弾発注を送信しない。
- 取引所API送信を実装しない。
- 秘密鍵や取引所APIキーを新しく扱わない。
- `opportunity_gate=pass` を `執行候補` と表示しない。
- `Phase 1B-lite` を正式 `Phase 1B` と扱わない。
- 成績が出る前にスコアを大きく緩めない。
- SL/TP 倍率を品質ガード前に広げない。
