# Ver03-v1 Codex再開引き継ぎ

更新日: 2026-06-08 JST  
対象repo: `afrogdesign/BTC_FX_CODEX`  
対象branch: `Ver03-v1`  
作成目的: 別の Codex が、現時点の続きから誤認なく作業を再開できるようにする。

## 1. この引き継ぎ資料の位置づけ

この文書は、Ver03-v1 の現時点の到達点、未完了事項、runtime 確認結果、次に読むべき資料、次作業の分岐条件を固定するための handoff である。

この文書を読む Codex は、まずここに書かれた事実を前提にし、その後で必要最小限の関連資料だけを読むこと。

## 2. 再開時の固定事実

2026-06-08 JST 時点の固定事実は以下。

- 作業ディレクトリ: `/Users/marupro/CODEX/01_active/BTC_FX_CODEX/btc_monitor`
- 作業ブランチ: `Ver03-v1`
- 確認済み HEAD: `97c0c9b`
- 直近完了作業番号: `BTCFX-20260608-038`
- 直近 commit:
  - `97c0c9b` `Record Active Plan runtime status`
- 2026-06-08 時点では worktree は clean で終了している。
- 既知の未追跡ファイル問題は `BTCFX-20260608-036` で整理済み。
- Active Plan 候補 CSV レーン実装は完了済み。
- ただし runtime 実データ上では `logs/csv/active_plan_candidates.csv` はまだ未生成確認。
- よって、次に必要なのは「監視サイクルが Ver03-v1 / 032 以降のコードで実際に走っているか」の確認である。

## 3. まず最初に読むファイル

別 Codex が再開するときは、以下の順で読む。

1. `AGENTS.md`
2. `運用資料/NEXT_TASK.md`
3. `運用資料/計画/Ver03-v1_現行資産棚卸し_20260607.md`
4. `運用資料/計画/Ver03-v1_ActivePlan_runtime確認手順_20260608.md`
5. `運用資料/作業ログ/BTCFX-20260608-038_active_plan_runtime_status.md`
6. `運用資料/reports/analysis/active_trade_plan_diagnostics_20260608.md`

この 6 ファイルを読めば、Ver03-v1 の現在地と次の確認項目が分かるようにしてある。

## 4. ここまでの完了作業

### 4.1 2026-06-07 系

- `BTCFX-20260607-031`
  - commit: `fad5316`
  - 内容: `Ver03-v1_現行資産棚卸し_20260607.md` を作成
- `BTCFX-20260607-032`
  - 内容: 過去スレッド上の作業番号であり、本 handoff の直接継続点ではない
- `BTCFX-20260607-033` から `BTCFX-20260607-038`
  - Active Trade Plan core、payload 接続、notification context、件名・HTML 優先化、CSV列、diagnostics / effectiveness 追加
- `BTCFX-20260607-043` から `BTCFX-20260607-049`
  - Active Plan candidates / outcomes / report / daily-sync / registry 系の初期整備
- `BTCFX-20260607-050` から `BTCFX-20260607-053`
  - intraperiod 設計・repo 調査・最小 builder 追加
- `BTCFX-20260607-054`
  - commit: `421b925`
  - 内容: P0/P1 取り込み計画を固定
- `BTCFX-20260607-055`
  - commit: `5ac57a0`
  - 内容: `counter_long_short_watch` の paper position 保存 setup 修正
- `BTCFX-20260607-056`
  - commit: `97b6ec6`
  - 内容: paper diagnostics を filled-only 主指標へ分離
- `BTCFX-20260607-057`
  - commit: `7d37b1a`
  - 内容: P0 後の実データ確認 report 追加
- `BTCFX-20260607-058`
  - commit: `4889b09`
  - 内容: formal candidate の hard quality blocker 化
- `BTCFX-20260607-059`
  - commit: `9b35dbf`
  - 内容: Ver03-v1 進捗ボード HTML 追加
- `BTCFX-20260607-060`
  - commit: `3a91e0b`
  - 内容: 出来高 trigger の方向付き化
- その後
  - `e11eeeb`
  - 内容: 進捗ボードを repo 運用区切り向けに更新

### 4.2 2026-06-08 系

- `BTCFX-20260608-032`
  - commit: `2546081`
  - 内容: `active_plan_candidates.csv` の logging レーン追加
- `BTCFX-20260608-033`
  - commit: `bb0208f`
  - 内容: `active_trade_plan_diagnostics_YYYYMMDD.md` builder / CLI 追加
- `BTCFX-20260608-034`
  - commit: `f75b40a`
  - 内容: `NEXT_TASK.md` を Ver03-v1 現在地へ同期
- `BTCFX-20260608-035`
  - commit: `662da68`
  - 内容: 既知の未追跡ファイル 3 件を棚卸し
- `BTCFX-20260608-036`
  - commit: `8d23f21`
  - 内容: 未追跡ファイル 2 件を repo 外へ退避し、参考資料 1 件を採用
- `BTCFX-20260608-037`
  - commit: `113e7c6`
  - 内容: Active Plan runtime 確認手順書を追加
- `BTCFX-20260608-038`
  - commit: `97c0c9b`
  - 内容: runtime 状態を読み取り確認し、未生成状態を記録

## 5. 現在の実装状態

### 5.1 実装済み

- `src/trade/active_plan.py` に `active_trade_plan` 生成ロジックあり
- `main.py` は `active_trade_plan` を payload に接続済み
- `trades.csv` への Active Plan 主要列追加は設計上完了済み
- `src/storage/csv_logger.py` に `append_active_plan_candidates()` 実装済み
- `main.py` で `append_trade_log()` の後に `append_active_plan_candidates()` 接続済み
- `tools/log_feedback.py` に `build_active_trade_plan_diagnostics_report()` 実装済み
- `python tools/log_feedback.py --build-active-trade-plan-diagnostics --active-plan-report-date YYYYMMDD` で診断生成可能

### 5.2 まだ runtime で確認できていないこと

- `logs/csv/active_plan_candidates.csv` の実生成
- 実候補行の存在
- 実候補が入った状態での `active_trade_plan_diagnostics_YYYYMMDD.md` 再生成
- `active_plan_candidate_outcomes` の正本化
- Active Plan 診断の daily-sync 接続

## 6. 2026-06-08 時点の runtime 確認結果

`運用資料/作業ログ/BTCFX-20260608-038_active_plan_runtime_status.md` に基づく確認結果は以下。

### 6.1 active_plan_candidates.csv

- `logs/csv/active_plan_candidates.csv`: 未生成

### 6.2 trades.csv

- `logs/csv/trades.csv`: 存在
- 行数: `2076`
- 最新 `signal_id`: `20260608_090500`
- 最新 `timestamp_jst`: `2026-06-08T18:05:00.604985+09:00`
- header に以下が存在しない確認:
  - `active_plan_version`
  - `active_primary_action`
  - `active_subject_label`
  - `active_headline`
  - `active_trade_plan_json`
- 最新行でも Active Plan 関連値は確認できていない

### 6.3 runtime log

- `logs/last_result.json`: 存在
- mtime: `2026-06-08 18:05:47 +0900`
- `logs/runtime/monitor.err`: 空
- `logs/runtime/feedback_daily_sync.err`: 空

### 6.4 現時点の解釈

現時点で最も重要なのは以下。

- runtime error は見えていない
- しかし `active_plan_candidates.csv` は未生成
- `trades.csv` の最新行にも Active Plan 列の反映が見えていない

したがって、現時点では以下の可能性が高い。

1. 032 以降のコードで監視サイクルがまだ走っていない
2. 常駐中プロセスが古いコードのまま動いている
3. 最新 commit は repo にあるが、runtime 実体に反映されていない

## 7. 次の Codex が最優先でやること

次の Codex は、実装追加から始めてはいけない。  
まず runtime 側の事実確認を優先する。

最優先タスクは以下。

1. `Ver03-v1` / 最新 commit が監視サイクル実体に反映されているか確認する
2. `logs/csv/active_plan_candidates.csv` が生成されるか確認する
3. 生成後に Active Plan 診断を再生成する
4. 候補 CSV に実データが入ったことを確認してから、candidate outcomes / daily-sync 側へ進む

## 8. 再開時の実行コマンド

再開直後は、まず repo 状態を確認する。

```bash
git status --short --branch
git rev-parse --short HEAD
```

次に runtime 状態を見る。

```bash
ls -l logs/csv/active_plan_candidates.csv || true
tail -n 5 logs/csv/trades.csv || true
tail -n 5 logs/csv/active_plan_candidates.csv || true
ls -l logs/last_result.json || true
tail -n 50 logs/runtime/monitor.err || true
tail -n 50 logs/runtime/feedback_daily_sync.err || true
```

必要なら `.venv312` で `trades.csv` header / 最新行を確認する。

```bash
./.venv312/bin/python - <<'PY'
from pathlib import Path
import csv

path = Path("logs/csv/trades.csv")
if not path.exists():
    print("trades.csv: missing")
    raise SystemExit(0)

with path.open("r", newline="", encoding="utf-8") as fp:
    reader = csv.DictReader(fp)
    rows = list(reader)
    fields = reader.fieldnames or []

print(f"trades.csv rows: {len(rows)}")
for key in [
    "active_plan_version",
    "active_primary_action",
    "active_subject_label",
    "active_headline",
    "active_trade_plan_json",
]:
    print(f"has {key}: {key in fields}")

if rows:
    latest = rows[-1]
    print("latest signal_id:", latest.get("signal_id", ""))
    print("latest timestamp_jst:", latest.get("timestamp_jst", ""))
    print("latest active_plan_version:", latest.get("active_plan_version", ""))
    print("latest active_primary_action:", latest.get("active_primary_action", ""))
    print("latest active_trade_plan_json present:", bool(str(latest.get("active_trade_plan_json", "")).strip()))
PY
```

`active_plan_candidates.csv` が存在する場合だけ diagnostics を再生成する。

```bash
./.venv312/bin/python tools/log_feedback.py --build-active-trade-plan-diagnostics --active-plan-report-date 20260608
sed -n '1,140p' "運用資料/reports/analysis/active_trade_plan_diagnostics_20260608.md"
```

## 9. 次作業の分岐条件

### 9.1 候補CSVが生成され、候補行もある場合

次に進んでよい。

- `active_plan_candidate_outcomes` builder / CLI の正本化
- Active Plan 診断 / outcomes の daily-sync 接続
- 必要なら report hub 参照確認

### 9.2 候補CSVはあるが header only の場合

実装は入っているが、候補条件がまだ発生していない。  
この場合は、観測継続または候補発生条件の再現確認が必要。

### 9.3 候補CSVが未生成で、trades 側も Active Plan blank の場合

最優先は runtime 実体の確認。

- 監視サイクルが本当に最新 commit で動いているか
- 実行中プロセスが古い版ではないか
- 再起動が必要な設計かどうか

この確認なしに outcomes 実装へ進まないこと。

## 10. 今回の handoff で明示しておく注意

- この handoff は「今どこで止まっているか」を固定する資料である
- 現時点では「実装不足」より「runtime 未反映 / 未確認」の可能性のほうが高い
- したがって、次の Codex は先に runtime 事実確認を行うこと
- 監視プロセスの起動・停止・再起動は、明示指示がない限り実施しないこと
- 実弾発注 API、取引所 API キー、秘密鍵、自動注文送信には触れないこと

## 11. この handoff 作成時点の最終状態

- branch: `Ver03-v1`
- HEAD: `97c0c9b`
- 直近作業: `BTCFX-20260608-038`
- 作業の主停止点:
  - `active_plan_candidates.csv` runtime 未生成確認
  - `trades.csv` 最新行に Active Plan 列未確認
  - diagnostics 再生成は未実施

次の Codex は、この文書と `BTCFX-20260608-038_active_plan_runtime_status.md` を起点に再開すること。
