# Ver03-v1 再設計作業プロンプト集

更新日: 2026-06-07 JST

このファイルは `Ver03-v1` ブランチ開始時の作業指示入口である。

ChatGPT が司令塔、Codex は作業員。
Codex は自由判断をしない。各プロンプトの範囲だけを実行し、完了時に必ず Markdown の作業結果を残して GitHub に push する。

## 作業原則

- ブランチ: `Ver03-v1`
- 既存の `trade_execution_gate` は、初期段階では壊さない。
- 取引所への実弾発注 API は実装しない。
- まず計画・診断・表示・紙検証を再設計する。
- すべての作業完了時に `運用資料/作業ログ/` または `運用資料/reports/` に Markdown を残す。
- 実装判断は ChatGPT が行う。Codex は推測で設計変更しない。

## CODEX 作業プロンプト 01: Ver03-v1 計画棚卸しと現行資産診断

以下をそのまま実行してください。自由判断で設計を変更しないでください。

### 目的

Ver03-v1 の大改修に入る前に、現行 repo の計画 md、レポート、実装済み資産を棚卸しし、残す資産・捨てる資産・再設計する資産を Markdown に整理する。

### 対象ブランチ

`Ver03-v1`

### 必ず読むファイル

- `運用資料/NEXT_TASK.md`
- `運用資料/開発ロードマップ.md`
- `運用資料/計画/マイルストーン定義.md`
- `運用資料/計画/20260607_active_trade_plan_実用化計画.md`
- `運用資料/reports/report_hub_latest.md`
- `運用資料/reports/feedback_daily_sync_20260607.md`
- `運用資料/reports/analysis/market_map_effectiveness_20260607.md`
- `運用資料/reports/analysis/paper_opportunity_diagnostics_20260607.md`
- `運用資料/reports/analysis/paper_entry_sl_wait_redesign_20260607.md`
- `運用資料/reports/analysis/quality_guard_effectiveness_20260607.md`
- `運用資料/reports/analysis/soft_risk_collateral_damage_20260607.md`
- `src/analysis/market_map.py`
- `src/analysis/scoring.py`
- `src/analysis/rr.py`
- `src/trade/activation.py`
- `src/trade/execution_gate.py`
- `src/trade/observation_gate.py`
- `src/trade/opportunity_gate.py`
- `src/trade/phase1b_lite.py`
- `main.py`
- `src/presentation/sanitize.py`
- `src/notification/detail_page.py`
- `src/ai/summary.py`

### 出力ファイル

`運用資料/計画/Ver03-v1_現行資産棚卸し_20260607.md`

### 出力内容

以下の見出しを必ず入れる。

1. `# Ver03-v1 現行資産棚卸し`
2. `## 1. 結論`
3. `## 2. 残すべき資産`
4. `## 3. 再設計する資産`
5. `## 4. 廃止または降格する考え方`
6. `## 5. 現行 gate が実用トレードに向かない理由`
7. `## 6. BTC 実務に必要な新機能`
8. `## 7. Ver03-v1 の最初の実装候補`
9. `## 8. 未解決の確認事項`

### 判断基準

- `trade_execution_gate=pass` が 0 件である現実を問題として扱う。
- ただし `trade_execution_gate` は高信頼 gate として残す。
- `market_map`、support/resistance、entry zone、SL/TP、paper_positions、report hub は原則残す方向で評価する。
- 「危ないから入るな」だけの通知になっていないかを重点的に見る。
- 「毎回どう構えるか」を出すために必要な不足項目を列挙する。

### 完了条件

- Markdown を作成する。
- `git diff --check` を実行する。
- 可能なら既存テストを実行する。全件が重い場合は、変更が md のみであることを明記してテスト省略可。
- commit message: `Add Ver03-v1 asset inventory`
- `Ver03-v1` に push する。

### 作業後に報告すること

- 作成したファイルパス
- commit SHA
- 実行した検証コマンド
- テスト未実行なら理由

## CODEX 作業プロンプト 02: Ver03-v1 新ロードマップ草案

プロンプト01が完了してから実行してください。

### 目的

既存のロードマップを、実務で使える BTC トレード支援システムへ再定義する。正式 gate ゼロ問題を前提に、Active Trade Plan を中心とした Ver03-v1 ロードマップを作る。

### 対象ブランチ

`Ver03-v1`

### 入力として読むファイル

- `運用資料/計画/Ver03-v1_現行資産棚卸し_20260607.md`
- `運用資料/計画/20260607_active_trade_plan_実用化計画.md`
- `運用資料/開発ロードマップ.md`
- `運用資料/計画/マイルストーン定義.md`

### 出力ファイル

`運用資料/計画/Ver03-v1_実用トレード支援ロードマップ_20260607.md`

### 出力内容

以下の見出しを必ず入れる。

1. `# Ver03-v1 実用トレード支援ロードマップ`
2. `## 1. Ver03-v1 の目的`
3. `## 2. 旧方針から変更する点`
4. `## 3. 新フェーズ定義`
5. `## 4. Active Trade Plan の役割`
6. `## 5. 通知が毎回出すべき行動計画`
7. `## 6. 紙検証と実務通知の分離`
8. `## 7. 自動化へ進む条件`
9. `## 8. 実弾発注をまだ禁止する理由`
10. `## 9. Codex 実装順序`

### 重要な方針

- `FORMAL_GO`、`ACTIVE_MARKET_SMALL`、`ACTIVE_LIMIT_RETEST`、`ACTIVE_BREAKOUT_FOLLOW`、`ACTIVE_COUNTER_SCALP`、`POSITION_MANAGEMENT`、`NO_ACTION` を定義する。
- `NO_ACTION` は最後の分類とし、通常は何らかの構えを出す。
- 自動発注ではなく、まず人間が使える行動計画と紙検証を作る。
- 実弾発注 API、秘密鍵、取引所 API 注文送信は明確に禁止する。

### 完了条件

- Markdown を作成する。
- `git diff --check` を実行する。
- commit message: `Add Ver03-v1 practical trading roadmap`
- `Ver03-v1` に push する。

## CODEX 作業プロンプト 03: active_plan.py 最小実装仕様とテストだけ追加

プロンプト02が完了してから実行してください。

### 目的

本体通知や gate を変えず、まず `src/trade/active_plan.py` と `tests/test_active_trade_plan.py` だけを追加して、Active Trade Plan の中核判定を固定する。

### 対象ブランチ

`Ver03-v1`

### 実装ファイル

- `src/trade/active_plan.py`
- `tests/test_active_trade_plan.py`
- 作業結果: `運用資料/作業ログ/Ver03-v1_active_plan_minimal_20260607.md`

### 必須関数

```python
def classify_zone_position(side: str, price: float, entry_low: float, entry_high: float) -> str:
    ...

def rr_for_entry(side: str, entry_price: float, stop: float, target: float) -> float | None:
    ...

def build_active_trade_plan(...):
    ...
```

`build_active_trade_plan` の引数は、最低限以下を受け取る。

```python
current_price: float
bias: str
market_regime: str
long_setup: dict
short_setup: dict
confidence_direction_shadow: float
confidence_execution_shadow: float
confidence_wait_shadow: float
risk_flags: list[str]
market_map_flags: list[str]
no_trade_flags: list[str]
data_quality_flag: str
breakout_up: bool
breakout_down: bool
volume_ratio: float
trigger_volume_ratio_threshold: float
```

### 20260606_230500 型 fixture の期待

以下の値でテストする。

```python
current_price = 60513.0
bias = "short"
long_setup = {
    "entry_zone": {"low": 60322.42, "high": 60524.08},
    "entry_mid": 60423.25,
    "stop_loss": 60025.57,
    "tp1": 60940.23,
    "tp2": 61377.68,
    "status": "watch",
}
short_setup = {
    "entry_zone": {"low": 60680.82, "high": 60882.88},
    "entry_mid": 60781.85,
    "stop_loss": 61179.73,
    "tp1": 60264.61,
    "tp2": 59826.94,
    "status": "invalid",
}
confidence_execution_shadow = 18.0
confidence_wait_shadow = 59.2
risk_flags = ["short_into_major_support", "short_at_major_support_wait_only"]
market_map_flags = ["support_to_resistance_flip", "major_resistance_rejection", "trend_flip_early_down"]
```

期待:

- `primary_action` は `ACTIVE_LIMIT_RETEST` または `ACTIVE_COUNTER_SCALP` を含む扱いにする。
- short side の `market_entry_status` は `blocked`。
- short side の `limit_entry_status` は `allowed`。
- long side の `counter_scalp_status` は `conditional`。
- headline は「成行ショート不可」「戻り売り待ち」「短期反発帯」の意味を含む。

### 禁止

- `main.py` はまだ変更しない。
- 通知 HTML はまだ変更しない。
- 既存 gate は変更しない。

### 検証

- `python -m unittest tests.test_active_trade_plan`
- 可能なら `python -m unittest discover -s tests -p 'test*.py'`
- `git diff --check`

### commit

`Add active trade plan core`

## CODEX 作業プロンプト 04: main.py へ active_trade_plan を接続

プロンプト03が完了してから実行してください。

### 目的

`active_trade_plan` を `core_result` に追加する。ただし通知件名、HTML、gate、paper order の挙動はまだ変えない。

### 変更対象

- `main.py`
- 必要なら `src/storage/csv_logger.py`
- 作業結果: `運用資料/作業ログ/Ver03-v1_active_plan_payload_20260607.md`

### 要件

- `from src.trade.active_plan import build_active_trade_plan` を追加。
- `core_result` が `build_notification_context()` に渡る前に active plan を生成する。
- `core_result["active_trade_plan"]` を追加。
- `core_result["active_primary_action"]` を追加。
- `core_result["active_headline"]` を追加。
- 既存の `trade_execution_gate`、`paper_order_status`、`opportunity_gate` の判定は変更しない。

### 検証

- `python -m unittest tests.test_active_trade_plan`
- 可能なら `python -m unittest discover -s tests -p 'test*.py'`
- `git diff --check`

### commit

`Wire active trade plan payload`

## CODEX 作業プロンプト 05: 通知件名と HTML hero を active plan 優先にする

プロンプト04が完了してから実行してください。

### 目的

通知を「方向」ではなく「取れる行動」から読めるようにする。

### 変更対象

- `src/ai/summary.py`
- `src/presentation/sanitize.py`
- `src/notification/detail_page.py`
- 必要に応じてテスト追加
- 作業結果: `運用資料/作業ログ/Ver03-v1_active_notification_ui_20260607.md`

### 要件

- 件名は active headline を優先する。
- hero の最上段に以下を表示する。
  - 今すぐ成行
  - 指値/戻り待ち
  - ブレイク追随
  - 逆方向短期
  - 保有中なら
- 方向スコアは下段へ移動または強調を弱める。
- `FORMAL_GO` と `ACTIVE_*` は混同しない。
- `ACTIVE_*` は「実弾自動発注」ではなく「人間の行動計画」として表示する。

### 20260606_230500 型の期待件名

完全一致でなくてよいが、以下の意味を含める。

```text
成行ショート不可 / 戻り売り待ち / 現値は短期反発帯
```

### 検証

- 関連テスト
- `python -m unittest discover -s tests -p 'test*.py'`
- `git diff --check`

### commit

`Prioritize active plan in notifications`

## CODEX 作業プロンプト 06: active_trade_plan_diagnostics を追加

プロンプト05が完了してから実行してください。

### 目的

Active Trade Plan が本当に実務に使えるか、過去ログで後付け診断できるようにする。

### 変更対象

- `tools/log_feedback.py`
- `運用資料/reports/analysis/active_trade_plan_diagnostics_20260607.md` を生成
- 作業結果: `運用資料/作業ログ/Ver03-v1_active_plan_diagnostics_20260607.md`

### 集計項目

- `active_primary_action` 別件数
- `ACTIVE_MARKET_SMALL` 件数
- `ACTIVE_LIMIT_RETEST` 件数
- `ACTIVE_COUNTER_SCALP` 件数
- side 別件数
- 成行 allowed の TP1 / SL / missed / avg_R
- 指値 allowed の TP1 / SL / missed / avg_R
- counter scalp の TP1 / SL / missed / avg_R
- `NO_ACTION` の割合
- 1日あたり候補数

### 注意

既存ログに active plan がない場合は、後付け計算または「現時点では新規ログ以降のみ」と明記する。

### 検証

- `python -m unittest tests.test_log_feedback`
- `python -m unittest discover -s tests -p 'test*.py'`
- `git diff --check`

### commit

`Add active trade plan diagnostics`
