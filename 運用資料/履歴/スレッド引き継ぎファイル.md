更新日: 2026-04-10 05:30 JST

# スレッド引き継ぎファイル

このファイルは、次スレッドで `btc_monitor` の確認作業を安全に再開するための例外用メモです。  
通常時は `README.md` → `NEXT_TASK.md` → `開発ロードマップ.md` までで足ります。ここは、環境の取り違え、本番/開発の混同、archive・sandbox・停止系の判断が必要なときだけ見ます。

## いま残す前提

- 現行の開発主流は `iMac 2019` 側の `Ver02.4-v1`
- AI 事後評価は `daily-sync` と `sync-ai-post-reviews` の2段運用
- `user_reviews.csv` は AI 正本、人の修正は `human_override`
- `MBP2020` の旧 `Ver02.1` 側は凍結済みの履歴として扱う
- `Ver02.3` 時代のレポート群は履歴資料であり、現行判断の正本ではない

## このファイルを見る場面

- iMac の開発環境と MBP2020 の本番環境を取り違えそうなとき
- 旧 archive や sandbox を使う必要があるとき
- 停止済み `Ver02.1` の扱いで迷ったとき
- 「どの資料が現行で、どれが履歴か」を即確認したいとき

## 今の確認順

1. `運用資料/README.md`
2. `運用資料/NEXT_TASK.md`
3. `運用資料/開発ロードマップ.md`
4. 必要なら `運用資料/計画/AI事後評価運用_Ver02.4-v1.md`
5. 必要なら `運用資料/計画/Phase1条件の見方.md`

## 環境の扱い

- 開発中の稼働確認は、まず「いま見ているのが iMac か MBP2020 か」を確定する
- `Global_BOX` の `本番環境.md` と `iMac 2019.md` は混ぜて読まない
- iMac 上で動いているからといって、本番反映済みとは扱わない

## archive と sandbox

- 強制確認が必要なときは、本流ではなく sandbox `/Users/marupro/CODEX/BTC_FX_CODEX_sandbox/btc_monitor` を使う
- 本流比較環境の `logs/`、CSV、`signals`、`last_result.json`、`heartbeat.txt` は汚さない
- 旧 `Ver02.1` archive の確認は、現行 `Ver02.4-v1` の判断資料と混ぜない
