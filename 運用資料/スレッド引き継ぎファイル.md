# スレッド引き継ぎファイル

更新日: 2026-03-16 19:36 JST

このファイルは、次スレッドで `btc_monitor` の確認作業を安全に再開するための例外用引き継ぎメモです。
通常時は `NEXT_TASK.md` → `開発ロードマップ.md` までで足りる構成を優先し、このファイルは節目完了時、運用前提変更時、ブロッカー停止時、次の AI が迷いそうなときだけ開きます。

## 現状サマリ
- 現在の入口ブランチは `main`、開発ブランチは `ver02.1-v2`。`ver02.1-stable` は今回の安定固定版、`snapshot/ver02.1` は固定時点、`ver01-baseline` は比較基準として運用する。
- 現在版は `Ver02.1`。本番は `Ver02.1 API`、開発は `Ver02.1 CLI` に役割を固定した。
- メール件名は `[Ver02.1] [API] [BTC監視] ...` / `[Ver02.1] [CLI] [BTC監視] ...` を基本形に統一済み。
- Ver01 の比較役目は完了し、`2026-03-16 19:14 JST` に本番常駐 `com.afrog.btc-monitor-ver01` を停止済み。今後の実装・計画・改善の主対象は Ver02.1。
- 次の大型節目は `Ver03`。条件は「`Phase 0` 通知後フロー一周完了」と「`Phase 1` 中核の実データ確認」が両方そろうこと。
- `運用資料/計画/` を実装判断の正本として再編済み。次の AI は、まず `フェーズ別計画_Phase0-1.md`、次に `フェーズ別計画_Phase2-3.md`、最後に `マイルストーン定義.md` を見れば判断できる。
- Ver02.1 には、`loss_streak` 自動計算、`phase1_active`、`phase1_activation_reason`、`position_sizing`、`exit_manager`、`shadow_log.csv` / レポート接続、`api / cli` 独立切替、件名での `[API]` / `[CLI]` 表示まで入っている。
- CLI 側は `codex` 実行パス自動解決と再試行まで実装済み。sandbox `/Users/marupro/CODEX/BTC_FX_CODEX_sandbox/btc_monitor` では 6 サイクル連続で `ai_decision` 欠落なし、`summary_body` 正常、`logs/errors/` 空を確認済み。
- Phase 1 の正式指標は、「本有効件数 (`n`) / TP1 到達率 / `tp1_hit_first=false` 率 / `expired` 率 / 平均 `risk_percent_applied` / 連敗時平均 `risk_percent_applied` / `max_size_capped` 発生率」で整理済み。
- ただし、`expired` と `tp1_hit_first=false` は現状 proxy 指標で、timeout / stop の厳密実測ログではない。
- 本番 Ver02.1 は `/Users/marupro/CODEX/BTC_FX_CODEX_ver02/btc_monitor` に配備済みで、`com.afrog.btc-monitor-ver021` として運用する。実体パスはログ保全のため従来の `ver02` ディレクトリを継続利用する。
- Ver01 本番の実体は `/Users/marupro/CODEX/BTC_FX_CODEX_ver01/btc_monitor` に保持しているが、`com.afrog.btc-monitor-ver01` 常駐は停止済み。必要時だけ再開確認に使う。
- `Global_BOX` は `README.md` を入口にし、`10_共通仕様` / `20_初期構築` / `30_テンプレート` に整理済み。共通仕様は `10_共通仕様`、案件生成は `20_初期構築`、ひな形は `30_テンプレート` を見る。
- 常駐の共通ルールは `Global_BOX/10_共通仕様/開発環境仕様書.md`、BTC 固有の現況は `運用/本番常駐メモ.md` を正本にする。
- 開発環境の常駐は `com.afrog.btc-monitor`。本番比較環境は観測専用のため、強制確認は sandbox 側だけで行う。
- コード反映は `tools/deploy_ver021_prod.sh`、本番状態の普段使い確認は `tools/sync_ver021_prod_status.sh`、詳細ログ取得は `tools/pull_ver021_prod_logs_auto.sh` を入口にする。
- `tmp/` は `status/`、`snapshots/`、`errors/` へ整理済み。日常確認は `tmp/status/`、詳細確認は `tmp/snapshots/` を使う。
- `pull_ver021_prod_logs_auto.sh` は鍵認証専用の標準入口で、パスワード fallback は `pull_ver021_prod_logs_with_password.sh` を明示的に使う例外手順へ分離した。
- 定期処理は Mac 側 `launchd` で `tools/sync_ver021_prod_status.sh` を 2 時間ごとに回す運用へ寄せる。AI の自動巡回は使わない。
- `com.afrog.btc-monitor-status-sync` は 2026-03-13 15:53 JST に登録済み。`launchctl print` で `run interval = 7200 seconds`、`last exit code = 0` を確認した。
- 普段の本番確認は `tmp/status/prod_status_summary.md` と `tmp/status/prod_status_sync_last_success.txt` を入口にし、`pull_ver021_prod_logs_auto.sh` は例外時だけ使う。
- 実データの正本は MBP2020 側で、`logs/` や CSV / JSON は Git に入れない。
- `signal_outcomes.csv` と `user_reviews.csv` はまだ未生成で、通知後 24 時間の本番確認待ち。
- `NEXT_TASK.md` は判断用の短縮メモ、`progress.md` は軽い実行履歴入口という役割分離を固定した。`NEXT_TASK.md` に時系列実施ログを書かない。
- 重い実施履歴は `progress_weekly/` へ週ごとに退避する。
- 2026-03-13 14:57 JST 時点で API 側 snapshot は `20260313_050500`（14:05 JST）まで更新済み。11:05 JST 帯以降の同時刻比較母数を追加できた。
- `tools/pull_ver021_prod_logs.sh` は `BTC_MONITOR_PROD_SSH_PASSWORD` を使う非対話実行に対応済み。`Too many authentication failures` ブロッカーを回避できる。
- Obsidian 側 `👩‍⚖️秘書.md` への書き込みはこの実行環境で実施可能に戻った。

## 最優先タスク
1. Ver02.1 単独運用の自然更新を観測し、`heartbeat.txt` と `last_result.json` が継続して進むか確認する。
2. 通知や注意報が出たら、件名・本文・`notify_reason_codes`・AI助言の差を API / CLI で比較する。
3. 今後の本番反映は `zsh tools/deploy_ver021_prod.sh`、普段の本番確認は `zsh tools/sync_ver021_prod_status.sh`、詳細ログ取得は `zsh tools/pull_ver021_prod_logs_auto.sh` を入口にする。
4. 普段は `tmp/status/prod_status_summary.md` を見て判断し、詳細が必要なときだけ `last_result.json`、`logs/errors/`、CSV、`signals/*.json` を読む。
5. 最初の通知から 24 時間後に本番 Ver02.1 で `./.venv312_prod/bin/python tools/log_feedback.py daily-sync` を実行し、`signal_outcomes.csv`、`shadow_log.csv`、`📝通知レビュー.md` の初回更新を確認する。
6. 強制確認が必要なときは本流ではなく sandbox `/Users/marupro/CODEX/BTC_FX_CODEX_sandbox/btc_monitor` を使う。本流比較環境の `logs/` は汚さない。

## 再開条件
- AI は通常 `NEXT_TASK.md` → `開発ロードマップ.md` の順で確認し、足りないときだけこのファイルを使う。
- 直近の実施履歴は `progress.md`、過去の重い履歴は `progress_weekly/`、中期計画は `運用資料/計画/`、運用ルールの正本は `AGENTS.md`、記録ファイル相関は `運用/記録ファイル運用ルール.md` を参照する。
- ChatGPT API を使う作業は、実行前に必ずユーザー許可を取る。
- API 側 snapshot 追加取得時は、必要に応じて `BTC_MONITOR_PROD_SSH_PASSWORD` を設定して `zsh tools/pull_ver021_prod_logs.sh` を実行する。

## 重要ルール
- 強制確認が必要なときは本流ではなく sandbox `/Users/marupro/CODEX/BTC_FX_CODEX_sandbox/btc_monitor` を使う。
- 本流比較環境の `logs/`、CSV、`signals`、`last_result.json`、`heartbeat.txt` は汚さない。
- `ver02.0-stable` と `ver02.1-stable` は安定退避名、`snapshot/ver02.1` は固定時点名、`ver02.1-v2` は継続開発名として使い分ける。
