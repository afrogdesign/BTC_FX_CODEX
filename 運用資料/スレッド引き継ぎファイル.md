# スレッド引き継ぎファイル

更新日: 2026-03-12 11:36 JST

このファイルは、次スレッドで `btc_monitor` の確認作業を安全に再開するための引き継ぎメモです。
今後は日付ごとに増やさず、この固定ファイル名をスレッド切り替え直前や大きな区切りのときに精査して書き直します。
次スレッドでは、このファイルをユーザーがそのまま貼れば、現在の運用前提を引き継いだまま再開しやすい状態にしてあります。

## まず読む
- `/Users/marupro/Library/Mobile Documents/iCloud~md~obsidian/Documents/AFROG電脳/10_デジタルスキル/00_PROJECT/FX/トレード支援システム/👩‍⚖️秘書.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/開発ロードマップ.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/NEXT_TASK.md`
- ここまでで足りる場合は、この先を全部読まなくてよい。

## 必要なときだけ開く
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/計画/マイルストーン定義.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/計画/フェーズ別計画_Phase0-1.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/AGENTS.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/運用/今後の運用ルール.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/運用/ログ検証と改善運用ガイド.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/参考資料/AI向けシステムロジック全体整理.md`
- `/Users/marupro/Library/Mobile Documents/iCloud~md~obsidian/Documents/AFROG電脳/10_デジタルスキル/00_PROJECT/FX/トレード支援システム/📒打ち合わせノート.md`
- `/Users/marupro/Library/Mobile Documents/iCloud~md~obsidian/Documents/AFROG電脳/10_デジタルスキル/00_PROJECT/FX/トレード支援システム/📝通知レビュー.md`

補足:
- `progress.md` は履歴保管用です。次スレッドでは、経緯確認が必要なときだけ参照すれば足ります。
- 全体計画の正本は `運用資料/開発ロードマップ.md` と `運用資料/計画/` 配下です。Obsidian 側は入口と要約だけに保ちます。
- `運用資料/` 直下は毎回使う資料だけに絞り、運用手順は `運用/`、参考資料は `参考資料/` へ分けました。
- このファイルは圧縮版なので、重い背景説明や古い経緯は持ち込みすぎません。

## 現状サマリ
- ブランチは `ver02`。
- 常駐の正本は MBP2020 本番環境。MBA15 は開発・単発確認専用。
- Ver01 は比較基準、Ver02 は改善検証対象として MBP2020 で並走運用する前提。
- Ver02 には、ログ活用基盤 フェーズ1 と 改善設計書 v2.1 の Phase 0 拡張が実装済み。
- Ver02 の全体計画は `運用資料/開発ロードマップ.md` と `運用資料/計画/` 配下へ分割し、次スレッドで重い長文を毎回読み直さずに済む構成へ切り替えた。
- 現在の版位置づけは `Ver02.x`。次の大型節目は `Ver03` で、`Phase 0` 通知後フロー一周完了と `Phase 1` 中核導入を両方満たしたときに昇格する。
- `shadow_log.csv`、`tools/log_feedback.py`、`run_cycle()` の factor breakdown / reason codes / data quality 系追加は導入済み。
- 開発環境常駐 `com.afrog.btc-monitor` は停止済み。LaunchAgents からも退避済み。
- Ver02 本番は `/Users/marupro/CODEX/BTC_FX_CODEX_ver02/btc_monitor` に配備済みで、`com.afrog.btc-monitor-ver02` として運用。
- Ver01 本番は `/Users/marupro/CODEX/BTC_FX_CODEX_ver01/btc_monitor` / `com.afrog.btc-monitor-ver01` で継続稼働。
- `trades.csv` と `logs/signals/*.json` には新ログ列が入り始めている。
- 開発環境で蓄積した Ver02 ログのうち、`logs/csv/`、`logs/signals/`、`logs/cache/` は本番 Ver02 側へ移行済み。
- Obsidian 側には `👩‍⚖️秘書.md` を追加済みで、次スレッドではまずそこを入口として見れば現状把握しやすい。
- Git は AI 側で、差分確認、意味単位のコミット分割、Push まで自動管理する前提。

## 2026-03-12 10:44 JST 時点までの運用確認結果
- Ver02 本番 `com.afrog.btc-monitor-ver02` は MBP2020 で起動済み。起動確認時の `pid` は `98787`。
- Ver01 本番 `com.afrog.btc-monitor-ver01` は MBP2020 で継続稼働中。確認時の `pid` は `91182`。
- 開発環境ローカル常駐 `com.afrog.btc-monitor` は停止済み。
- `logs/csv/trades.csv` と `shadow_log.csv` の運用は本番 Ver02 側へ引き継ぎ済み。
- `logs/csv/signal_outcomes.csv` と `logs/csv/user_reviews.csv` は未生成。
- `trades.csv` の `was_notified=True` は 0 件で、通知済みシグナルはまだ発生していない。
- `👩‍⚖️秘書.md`、`開発ロードマップ.md`、`NEXT_TASK.md`、`📒打ち合わせノート.md`、この `スレッド引き継ぎファイル.md` は最新状態に更新済みで、入口・全体計画・直近実務・中長期メモの役割分担ができている。
- したがって、`daily-sync` の初回本番確認、`notify_reason_codes` の通知経路確認、`actual_move_driver` 入力後の `logic_validated` 実確認に加え、`Phase 1` 中核導入も `Ver03` 前の必須条件として残っている。

## 最優先タスク
1. 次の通知発生サイクルを確認し、Ver02 の `trades.csv` と `logs/signals/*.json` に `was_notified=True` と `notify_reason_codes` が実データで入るか確認する。
2. 通知が出たら、Ver01 / Ver02 の通知メール件名・本文・`notify_reason_codes`・runtime ログが混線していないか確認する。
3. 最初の通知から 24 時間後に本番 Ver02 で `./.venv312_prod/bin/python tools/log_feedback.py daily-sync` を実行し、`signal_outcomes.csv`、`shadow_log.csv`、`📝通知レビュー.md` の初回更新を確認する。
4. `📝通知レビュー.md` に `actual_move_driver` を入力し、`review_status=done` の取り込み後に `logic_validated` が `user_reviews.csv` と `shadow_log.csv` に反映されるか確認する。
5. 通知待ちと並行して、`src/trade/position_sizing.py` と `src/trade/exit_manager.py` の入出力定義、追加設定値、ログ保存項目を固める。
6. `Ver03` 昇格条件に照らして、`Phase 0` と `Phase 1` の未充足項目を `運用資料/計画/フェーズ別計画_Phase0-1.md` で定期確認する。

## 補足
- 先頭の数行に `signal_id` が空の旧ログが残っているが、`tools/log_feedback.py` は `signal_id` がない行を集計対象から外す実装。
- 次スレッドでは、まず `👩‍⚖️秘書.md` と `開発ロードマップ.md` で現在地を確認し、その後 `NEXT_TASK.md` と必要に応じてこの `スレッド引き継ぎファイル.md` を見る。
- `progress.md` は履歴、`NEXT_TASK.md` は直近実務、`開発ロードマップ.md` は全体入口、`運用資料/計画/` は中期計画、`📒打ち合わせノート.md` は中長期の判断材料、`👩‍⚖️秘書.md` は入口メモ、`スレッド引き継ぎファイル.md` は次スレッド用の圧縮引き継ぎとして使い分ける。
- ChatGPT API を使う作業は、実行前に必ずユーザー許可を取る。
