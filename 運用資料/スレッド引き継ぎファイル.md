# スレッド引き継ぎファイル

更新日: 2026-03-23 17:45 JST

このファイルは、次スレッドで `btc_monitor` の確認作業を安全に再開するための例外用引き継ぎメモです。
通常時は `NEXT_TASK.md` → `開発ロードマップ.md` までで足りる構成を優先し、このファイルは節目完了時、運用前提変更時、ブロッカー停止時、次の AI が迷いそうなときだけ開きます。

## 現状サマリ
- 現在版は `Ver02.1`。本番は `MBP2020`、軽量同期の常設先は `MBA M4`、`iMac 2019` には BTC 常駐を置かない。
- 本番 `.env` は `cli / cli` だが、実装は `CLI` 優先、失敗時だけ `API` 自動フォールバック。
- `Global_BOX` の正本は iCloud 側 `00_Global_BOX`。Mac 横断の実行一覧は `20_環境情報/実行ファイル/` を見る。
- BTC 側には案件固有の本番メモだけ残し、Mac 横断一覧の複製は持たない。
- 次の大型節目は `Ver03`。条件は「Phase 0 一周完了」と「Phase 1 中核の実データ確認」。
- 強制確認が必要なときは本流ではなく sandbox `/Users/marupro/CODEX/BTC_FX_CODEX_sandbox/btc_monitor` を使う。

## 最優先タスク
1. 本番の自然更新を観測し、`CLI` 失敗回だけ `API` へフォールバックしているかを確認する。
2. 通知や異常が出た回だけ本番詳細ログを掘り、件名、`system_mode_label`、AI エラーログを確認する。
3. レビュー反映を進めて `Phase 0` 一周完了へつなげる。

## 再開条件
- AI は通常 `NEXT_TASK.md` → `開発ロードマップ.md` の順で確認し、足りないときだけこのファイルを使う。
- 直近の実施履歴は `progress.md`、過去の重い履歴は `progress_weekly/`、中期計画は `計画/`、運用ルールの正本は `AGENTS.md`、記録ファイル相関は `運用/ルール/記録ファイル運用ルール.md` を参照する。
- ChatGPT API を使う作業は、実行前に必ずユーザー許可を取る。

## 重要ルール
- 強制確認が必要なときは本流ではなく sandbox `/Users/marupro/CODEX/BTC_FX_CODEX_sandbox/btc_monitor` を使う。
- 本流比較環境の `logs/`、CSV、`signals`、`last_result.json`、`heartbeat.txt` は汚さない。
- `ver02.0-stable` と `ver02.1-stable` は安定退避名、`snapshot/ver02.1` は固定時点名、`ver02.1-v2` は継続開発名として使い分ける。
