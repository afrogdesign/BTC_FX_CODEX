# スレッド引き継ぎファイル

更新日: 2026-03-13 12:40 JST

このファイルは、次スレッドで `btc_monitor` の確認作業を安全に再開するための引き継ぎメモです。
今後は日付ごとに増やさず、この固定ファイル名をスレッド切り替え直前や大きな区切りのときに精査して書き直します。
次スレッドでは、このファイルをユーザーがそのまま貼れば、現在の運用前提を引き継いだまま再開しやすい状態にしてあります。

## まず読む
- `/Users/marupro/Library/Mobile Documents/iCloud~md~obsidian/Documents/AFROG電脳/10_デジタルスキル/00_PROJECT/FX/トレード支援システム/👩‍⚖️秘書.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/開発ロードマップ.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/NEXT_TASK.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/AGENTS.md`
- ここまでで足りる場合は、この先を全部読まなくてよい。

## 必要なときだけ開く
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/計画/マイルストーン定義.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/計画/フェーズ別計画_Phase0-1.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/運用/今後の運用ルール.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/運用/ログ検証と改善運用ガイド.md`
- `/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/参考資料/AI向けシステムロジック全体整理.md`
- `/Users/marupro/Library/Mobile Documents/iCloud~md~obsidian/Documents/AFROG電脳/10_デジタルスキル/00_PROJECT/FX/トレード支援システム/📒打ち合わせノート.md`
- `/Users/marupro/Library/Mobile Documents/iCloud~md~obsidian/Documents/AFROG電脳/10_デジタルスキル/00_PROJECT/FX/トレード支援システム/📝通知レビュー.md`

補足:
- `progress.md` は履歴保管用です。次スレッドでは、経緯確認が必要なときだけ参照すれば足ります。
- 全体計画の正本は `運用資料/開発ロードマップ.md` と `運用資料/計画/` 配下です。Obsidian 側は入口と要約だけに保ちます。
- `運用資料/` 直下は毎回使う資料だけに絞り、運用手順は `運用/`、参考資料は `参考資料/` へ分けました。
- このファイルは圧縮版なので、重い背景説明や古い経緯は持ち込みすぎません。

## 現状サマリ
- 現在の入口ブランチは `main`、開発ブランチは `ver02.1-v1`。`snapshot/ver02.1` は固定時点、`ver01-baseline` は比較基準として運用する。
- 現在版は `Ver02.1`。本番は `Ver02.1 API`、開発は `Ver02.1 CLI` に役割を固定した。
- メール件名は `[Ver02.1] [API] [BTC監視] ...` / `[Ver02.1] [CLI] [BTC監視] ...` を基本形に統一済み。
- Ver01 は比較基準として残す。今後の実装・計画・改善の主対象は Ver02.1。
- 次の大型節目は `Ver03`。条件は「`Phase 0` 通知後フロー一周完了」と「`Phase 1` 中核の実データ確認」が両方そろうこと。
- `運用資料/計画/` を実装判断の正本として再編済み。次の AI は、まず `フェーズ別計画_Phase0-1.md`、次に `フェーズ別計画_Phase2-3.md`、最後に `マイルストーン定義.md` を見れば判断できる。
- Ver02.1 には、`loss_streak` 自動計算、`phase1_active`、`phase1_activation_reason`、`position_sizing`、`exit_manager`、`shadow_log.csv` / レポート接続、`api / cli` 独立切替、件名での `[API]` / `[CLI]` 表示まで入っている。
- CLI 側は `codex` 実行パス自動解決と再試行まで実装済み。sandbox `/Users/marupro/CODEX/BTC_FX_CODEX_sandbox/btc_monitor` では 6 サイクル連続で `ai_decision` 欠落なし、`summary_body` 正常、`logs/errors/` 空を確認済み。
- Phase 1 の正式指標は、「本有効件数 (`n`) / TP1 到達率 / `tp1_hit_first=false` 率 / `expired` 率 / 平均 `risk_percent_applied` / 連敗時平均 `risk_percent_applied` / `max_size_capped` 発生率」で整理済み。
- ただし、`expired` と `tp1_hit_first=false` は現状 proxy 指標で、timeout / stop の厳密実測ログではない。
- 本番 Ver02.1 は `/Users/marupro/CODEX/BTC_FX_CODEX_ver02/btc_monitor` に配備済みで、`com.afrog.btc-monitor-ver021` として運用する。実体パスはログ保全のため従来の `ver02` ディレクトリを継続利用する。
- Ver01 本番は `/Users/marupro/CODEX/BTC_FX_CODEX_ver01/btc_monitor` / `com.afrog.btc-monitor-ver01` で継続稼働し、ブランチ名は `ver01-baseline` として扱う。
- 開発環境の常駐は `com.afrog.btc-monitor`。本番比較環境は観測専用のため、強制確認は sandbox 側だけで行う。
- コード反映は `tools/deploy_ver021_prod.sh`、本番ログ取得は `tools/pull_ver021_prod_logs.sh` を入口にする。
- 実データの正本は MBP2020 側で、`logs/` や CSV / JSON は Git に入れない。
- `signal_outcomes.csv` と `user_reviews.csv` はまだ未生成で、通知後 24 時間の本番確認待ち。
- Obsidian 側の `👩‍⚖️秘書.md` は最新版へ更新済み。次スレッドではまずそこを見れば「いま何待ちか」と「次に何をするか」が分かる。
- 次スレッドでも `AGENTS.md` を確認し、入口順、記録更新ルール、ChatGPT API の事前許可ルールを先に再確認する。

## 2026-03-13 06:50 JST 時点の確認結果
- Ver02.1 本番 `com.afrog.btc-monitor-ver021` はデプロイ・再起動済み。次に必要なのは、新件名形式と新 launchd ラベルでの最初の自然サイクル観測。
- Ver01 本番 `com.afrog.btc-monitor-ver01` は比較基準として継続運用する。
- 開発環境の `com.afrog.btc-monitor` は `Ver02.1 CLI` の常駐確認対象。sandbox では安定したが、本流常駐での自然観測はまだ完了していない。
- `logs/csv/trades.csv` と `shadow_log.csv` の運用は本番 Ver02.1 側へ引き継ぎ済み。
- `logs/csv/signal_outcomes.csv` と `logs/csv/user_reviews.csv` はまだ未生成。
- `trades.csv` の `was_notified=True` はまだ 0 件で、通知済みシグナルは未発生。
- `phase1_active` などの新列はコードと集計側に入っているが、本番の実データ蓄積確認はこれから。
- いま残っている本命は「API 本番 / CLI 開発の自然観測」と「通知発生後の本番ログ確認」。

## 最優先タスク
1. 本番 Ver02.1 と開発 Ver02.1 の自然更新を観測し、`heartbeat.txt` と `last_result.json` が継続して進むか確認する。
2. API 本番と CLI 開発の通知が出たら、件名・本文・`notify_reason_codes`・AI助言の差を比較する。
3. 今後の本番反映は `zsh tools/deploy_ver021_prod.sh`、本番ログ確認は `zsh tools/pull_ver021_prod_logs.sh` を入口にする。
4. 次の通知発生サイクルを確認し、Ver02.1 の `trades.csv` と `logs/signals/*.json` に `was_notified=True` と `notify_reason_codes` が実データで入るか確認する。
5. 最初の通知から 24 時間後に本番 Ver02.1 で `./.venv312_prod/bin/python tools/log_feedback.py daily-sync` を実行し、`signal_outcomes.csv`、`shadow_log.csv`、`📝通知レビュー.md` の初回更新を確認する。
6. 強制確認が必要なときは本流ではなく sandbox `/Users/marupro/CODEX/BTC_FX_CODEX_sandbox/btc_monitor` を使う。本流比較環境の `logs/` は汚さない。

## 補足
- 先頭の数行に `signal_id` が空の旧ログが残っているが、`tools/log_feedback.py` は `signal_id` がない行を集計対象から外す実装。
- 次スレッドでは、まず `👩‍⚖️秘書.md` と `開発ロードマップ.md` で現在地を確認し、その後 `NEXT_TASK.md` と `AGENTS.md` を見れば運用前提までそろう。ここで足りなければこの引き継ぎファイルを開けば足りる。
- `progress.md` は履歴、`NEXT_TASK.md` は直近実務、`開発ロードマップ.md` は全体入口、`運用資料/計画/` は中期計画、`📒打ち合わせノート.md` は中長期の判断材料、`👩‍⚖️秘書.md` は入口メモ、`スレッド引き継ぎファイル.md` は次スレッド用の圧縮引き継ぎとして使い分ける。
- ChatGPT API を使う作業は、実行前に必ずユーザー許可を取る。
- `ver02.0-stable` は安定退避名、`snapshot/ver02.1` は固定時点名、`ver02.1-v1` は継続開発名として使い分ける（互換のため `codex/ver02.0-freeze` は残置）。
