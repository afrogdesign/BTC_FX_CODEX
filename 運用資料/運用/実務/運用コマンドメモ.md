# BTC Monitor 運用コマンドメモ

更新日: 2026-03-31 03:42 JST

いま本当に使うコマンドだけを残したメモです。

前提:
- 日常の主観測先は `iMac 2019`
- `MBP2020` の `Ver02.1` は 2026-03-31 に停止・凍結済み
- `MBA M4` は必要時の手動実行に寄せる

## 再発防止メモ

- `Ver02.3-OBS` の常設先は `iMac 2019`
- `MBA M4` は同じ `CODEX` パスを持つが、`Ver02.3-OBS` の常設確認先としては扱わない
- BTC Monitor の手動実行と backtest 再検証は、原則 `cd /Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor && .venv312/bin/python ...` を使う
- システムの `python3` は使わない。今回も `python3` では `pandas` 不足で失敗した
- 比較 CSV の再生成は MEXC からローソク足を取り直すため、ネット到達できない環境では失敗する
- 定刻実行確認は、まず `launchctl print gui/$(id -u)/com.afrog.btc-monitor`、次に `logs/heartbeat.txt`、`logs/last_result.json` の更新時刻を見る

## 最初に使う

```bash
cd /Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor
launchctl print "gui/$(id -u)/com.afrog.btc-monitor"
ls -lT logs/heartbeat.txt logs/last_result.json logs/runtime/monitor.out logs/runtime/monitor.err
```

用途:
- `iMac 2019` で `Ver02.3v3-OBS` の定刻実行を最短確認するとき

## archive 参照時の MBP2020 軽量確認

```bash
cd /Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor
zsh tools/sync_ver021_prod_status.sh
sed -n '1,120p' tmp/status/prod_status_summary.md
```

用途:
- 停止済み `Ver02.1` archive の状態や過去 snapshot を確認するとき

## archive の詳細取得

```bash
cd /Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor
zsh tools/pull_ver021_prod_logs_auto.sh
```

用途:
- `MBP2020` の過去ログを詳細に掘るとき

## archive からの復元メモ

```bash
ssh mbp2020-btc
cd /Users/marupro/CODEX
tar -xzf archive/BTC_FX_CODEX_ver02_20260331_0339.tgz
cp archive/com.afrog.btc-monitor-ver021.plist.20260331_0339 ~/Library/LaunchAgents/com.afrog.btc-monitor-ver021.plist
cd /Users/marupro/CODEX/BTC_FX_CODEX_ver02/btc_monitor
zsh tools/start_monitor_ver021_prod.sh /Users/marupro/CODEX/BTC_FX_CODEX_ver02/btc_monitor
```

用途:
- 停止済み `Ver02.1` を再開するときだけ使う

## 開発の単発実行

```bash
cd /Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor
.venv312/bin/python main.py
```

用途:
- 開発環境で手動確認するとき

## 比較 CSV の再生成

```bash
cd /Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor
.venv312/bin/python -
```

用途:
- backtest comparison を Codex から再実行するときの Python 入口確認

補足:
- 実際の再生成コードは都度変わりうるが、入口の Python は常に `.venv312/bin/python`
- `tmp/backtest/comparison_summary.csv` と `tmp/backtest_extended/comparison_summary.csv` は自動更新ではなく、必要時の手動再生成

## 事後評価

本番 MBP2020:

```bash
cd /Users/marupro/CODEX/BTC_FX_CODEX_ver02/btc_monitor
./.venv312_prod/bin/python tools/log_feedback.py daily-sync
```

用途:
- 通知から 24 時間後の評価同期

## よく見る場所

- 本番要約: `tmp/status/prod_status_summary.md`
- 本番生ログ: `logs/last_result.json`
- 本番 runtime エラー: `logs/runtime/monitor.err`
- 事後評価: `logs/csv/signal_outcomes.csv`
