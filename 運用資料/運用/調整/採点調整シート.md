更新日: 2026-04-02 04:20 JST

# 採点調整シート

判定が慎重すぎる、または攻めすぎると感じたときの早見表です。現行の主対象は `Ver02.3v4` です。

## 直近で効く主要項目

| 項目 | 現在値 | 見る場所 |
| --- | --- | --- |
| `CONFIDENCE_LONG_MIN` | `45` | `config.py` / `.env` |
| `CONFIDENCE_SHORT_MIN` | `55` | `config.py` / `.env` |
| `MIN_RR_RATIO` | `1.10` | `config.py` / `src/analysis/rr.py` |
| `POSITION_RISK_MEDIUM_THRESHOLD` | `55` | `config.py` |
| `POSITION_RISK_HIGH_THRESHOLD` | `80` | `config.py` |
| `ATTENTION_ALERT_SCORE_MIN` | `55` | `config.py` / `.env` |
| `ATTENTION_ALERT_GAP_MIN` | `15` | `config.py` / `.env` |
| `ATTENTION_ALERT_COOLDOWN_MINUTES` | `60` | `config.py` / `.env` |

## まず見る

1. `運用資料/reports/feedback_daily_sync_20260402.md`
2. `logs/signals/<signal_id>.json`
3. `logs/csv/shadow_log.csv`
4. `.env`
5. `config.py`

## 迷ったときの対応

- `prelabel=ENTRY_OK` なのに最終的に待機や invalid になる
  - `logs/signals/<signal_id>.json` で `prelabel` と `primary_setup_status` を確認する
  - `ENTRY_OK + invalid` が続くときは `src/analysis/confidence.py` の `entry_ok_invalid_penalty` を先に見る
  - 直近レポートで件数を確認する
- 方向は強いのに「実行しやすさ」が弱く「待機圧力」が高い
  - `confidence_direction_shadow`
  - `confidence_execution_shadow`
  - `confidence_wait_shadow`
  - `tools/log_feedback.py` の `direction_execution_conflict` を確認する
- ロングが強く出るのに実際は下向き優勢なことが多い
  - `signals_4h`
  - `position_risk_flags`
  - `countertrend_long_cluster` を確認する
  - `src/analysis/confidence.py` の `countertrend_long_cluster_*` 補正を確認する
- 無駄な通知が多い
  - `CONFIDENCE_*`
  - `ATTENTION_ALERT_*`
  - `src/notification/trigger.py`
- 初動で毎回待機になる
  - `src/analysis/position_risk.py`
  - `POSITION_RISK_MEDIUM_THRESHOLD`
  - `MIN_RR_RATIO`

## 今の優先論点

- `ENTRY_OK と setup invalid の整合性崩れ`
  - 2026-04-02 の週次レポートでは 9 件
- `速報で方向/実行不整合が継続`
  - 直近12時間で 3 件
- `countertrend_long_cluster`
  - 直近12時間で 4 件
- `bias=long` の direction wrong rate が高い
  - 直近レポートでは `83.3% (5/6)`

## 原則

- 単発の印象で動かさない
- まず週次レポートと `signals/*.json` の複数件を見る
- 実際に走る値は `config.py` より `.env` を優先して確認する
- 数値を動かす前に `3指標` と `primary_setup_status` の整合を確認する
