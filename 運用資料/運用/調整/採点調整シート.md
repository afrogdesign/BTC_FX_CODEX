更新日: 2026-03-23 18:00 JST

# 採点調整シート

判定が慎重すぎる、または攻めすぎると感じたときの早見表です。

## 直近で効く主要項目

| 項目 | 現在値 | 見る場所 |
| --- | --- | --- |
| `CONFIDENCE_LONG_MIN` | `40` | `config.py` / `.env` |
| `CONFIDENCE_SHORT_MIN` | `65` | `config.py` / `.env` |
| `MIN_RR_RATIO` | `1.15` | `config.py` / `src/analysis/rr.py` |
| `POSITION_RISK_MEDIUM_THRESHOLD` | `45` | `config.py` |
| `POSITION_RISK_HIGH_THRESHOLD` | `70` | `config.py` |
| `ATTENTION_ALERT_SCORE_MIN` | `52` | `config.py` / `.env` |
| `ATTENTION_ALERT_GAP_MIN` | `12` | `config.py` / `.env` |
| `ATTENTION_ALERT_COOLDOWN_MINUTES` | `30` | `config.py` / `.env` |

## まず見る

1. `logs/csv/trades.csv`
2. `logs/signals/<signal_id>.json`
3. `.env`
4. `config.py`

## 迷ったときの対応

- 方向は合っているのに通知が出ない
  - `CONFIDENCE_LONG_MIN`
  - `MIN_RR_RATIO`
  - `src/notification/trigger.py`
- 初動で毎回待機になる
  - `src/analysis/position_risk.py`
  - `POSITION_RISK_MEDIUM_THRESHOLD`
- 無駄な通知が多い
  - `CONFIDENCE_*`
  - `ATTENTION_ALERT_*`
  - `POSITION_RISK_HIGH_THRESHOLD`

## 原則

- 単発の印象で動かさない
- まず `trades.csv` と `signals/*.json` の複数件を見る
- 実際に走る値は `config.py` より `.env` を優先して確認する
