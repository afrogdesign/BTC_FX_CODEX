# ログ検証と改善運用ガイド

このファイルは、BTC監視システムのログを使って「何を確認し、どう改善するか」を定型化するための運用ガイドです。

## 1. 目的
- 監視ロジックの品質を、感覚ではなくログで評価する。
- 通知の質（多すぎる/少なすぎる）を改善する。
- 月次・年次で傾向を比較し、設定値の見直し根拠を作る。

## 2. まず見るファイル
- `logs/last_result.json`: 直近1回の判定詳細
- `logs/csv/trades.csv`: 連続実行の時系列ログ（集計の主対象）
- `logs/signals/*.json`: 各サイクルの詳細スナップショット
- `logs/errors/*.log`: 例外ログ

## 3. すぐ使える集計コマンド
実行場所:
```bash
cd /Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor
```

### 3-1. 全期間レポート（まずこれ）
```bash
python3 tools/log_analytics.py --output-md 運用資料/reports/log_review_all.md
```

### 3-2. 年次レポート
```bash
python3 tools/log_analytics.py --year 2026 --output-md 運用資料/reports/log_review_2026.md
```

### 3-3. 月次レポート
```bash
python3 tools/log_analytics.py --month 2026-03 --output-md 運用資料/reports/log_review_2026-03.md
```

### 3-4. 期間指定レポート
```bash
python3 tools/log_analytics.py --from 2026-03-01 --to 2026-03-31 --output-md 運用資料/reports/log_review_2026-03_range.md
```

## 4. レポートの見方
`tools/log_analytics.py` が出力する項目:
- 全体KPI
  - 平均 `confidence`
  - `primary_setup_status=invalid` 比率
  - `ai_error` 発生率
- 分布
  - `bias`, `phase`, `market_regime`, `primary_setup_status`
- `no_trade_flags` 上位
  - 何がボトルネックかを把握
- 年次集計 / 月次集計
  - 件数、平均confidence、invalid率、ai_error率
- 時間帯別（JST）
  - どの時間帯で品質低下が起きやすいか

## 5. 改善に直結する見方
### 5-1. invalid率が高いとき
- `RR_insufficient` が多いか確認。
- 多い場合は以下を優先検証:
  - `MIN_RR_RATIO`
  - S/R検出ロジック（ゾーン幅）
  - `SL_ATR_MULTIPLIER`

### 5-2. ai_error率が高いとき
- `AI_TIMEOUT_SEC` が短すぎないか確認。
- `AI_RETRY_COUNT` を増やす余地を確認。
- 回線・API障害時のフォールバック運用を見直し。

### 5-3. 偏りが強いとき（例: shortばかり）
- `market_regime` と一致しているかを確認。
- 一致していない場合、スコア閾値または構造判定ロジックを再評価。

## 6. 定例運用（おすすめ）
- 毎日: `last_result.json`, `errors` を軽く確認
- 毎週: 全期間レポートを更新して異常増加を確認
- 毎月: 月次レポートで設定変更の是非を判断
- 毎年: 年次レポートで運用方針を見直す

## 7. Codex への依頼テンプレ（このmdを指定して使う）
以下をそのまま使えます:

```text
/Users/marupro/CODEX/BTC_FX_CODEX/btc_monitor/運用資料/ログ検証と改善運用ガイド.md を使って、
最新ログの月次・年次レビューを実行してください。
改善優先度を3つに絞って、運用資料/NEXT_TASK.md に追記してください。
```

## 8. 将来の拡張（重要）
現状ログには実トレード損益（PnL）がないため、勝率や期待値の厳密評価はできません。
次の項目を将来ログへ追加すると、改善の質が大幅に上がります。

- エントリー価格
- エグジット価格
- 損益（絶対値・%）
- 勝敗フラグ
- 保有時間

## 9. 出力先
- レポート保存先: `運用資料/reports/`
- 例: `運用資料/reports/log_review_2026.md`
